{% set class = [
    'site-header',
    'position-relative',
]|merge(class|default([])) %}
<header {{ html_attr({ class }) }}>
    <div class="container">
        {% block content %}{% endblock %}
    </div>
</header>
